import ProductCard from "@/components/card/ProductCard";
import axios from "axios";
import Head from "next/head";
import React, { useState, useEffect } from "react";
import styles from "./manage-products.module.scss";

const manageProducts = () => {
  const [productState, setProductState] = useState([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    fetchAllProducts();
  }, []);

  const fetchAllProducts = async () => {
    setLoading(true);
    try {
      const res = await axios.get("/api/allproducts");
      console.log(res);

      setProductState(res.data.data);
      toast.success(`Successfully fetched all products`);
    } catch (err) {
      console.log(err);
    }
    setLoading(false);
  };

  return (
    <>
      <Head>
        <title>Graphene Pro - Admin</title>
        <meta name="description" content="Generated by create next app" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <link rel="icon" href="/images/logo_sm.png" />
      </Head>
      <main className="main">
        <div className={styles.allProductContainer}>
          <h1 className="subtitle">Manage All Products </h1>
          <div className={styles.allProducts}>
            {productState.map((pr, index) => (
              <ProductCard data={pr} idx={index} />
            ))}
          </div>
        </div>
      </main>
    </>
  );
};

export default manageProducts;
